# Kafka Configuration
KAFKA_BROKERS=localhost:9092
KAFKA_CLIENT_ID=event-service

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Application Configuration
NODE_ENV=development
PORT=3007

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Kafka Consumer Groups
NOTIFICATION_GROUP=notification-service-group
ANALYTICS_GROUP=analytics-service-group
EMAIL_GROUP=email-service-group
RECOMMENDATION_GROUP=recommendation-service-group
